{
  "name": "concord-ext-game-maker",
  "version": "0.1.0",
  "description": "Game Maker Protocol host extension (INS-007) — runs .game documents as Chat / Hybrid sessions",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "bundle": "pnpm build && node scripts/pack.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.5.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.4.0",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}
